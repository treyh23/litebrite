'use strict';

function main() {
  $('.select-color').on('click',() => {
    if(selectedColor === 'select-color cyan not-selected') {
      return 
    }  else if(selectedColor === '') {
      
    }
  }
)
};

$(document).ready(main);
